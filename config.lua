local host = "0.0.0.0"
local port = 8080

local root_path = "/home/oyasmi/var/org_html"           -- absolute or relative path

return {host = host,
        port = port,
        root_path = root_path}
